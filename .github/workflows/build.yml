name: Build ZMK firmware

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: config
      build_dir: build
      zephyr_cache: true
    secrets: inherit


